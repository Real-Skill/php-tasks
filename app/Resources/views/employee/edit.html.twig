{% extends 'base.html.twig' %}

{% block body %}
    <a class="button index" href="{{ path('employee_index') }}">Index</a>

    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {#{{ form_start(form) }}#}
    {#{{ form_widget(form) }}#}
    {#{{ form_end(form) }}#}
{% endblock %}

